package io.vieira.space;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfiguration {

    // TODO 1 : configure Spring MVC to allow CORS requests from domains "app-a.nasa.gov", "tracking.roscosmos.ru" and "internal.spacex.com", on all /api mappings

    // TODO 3 : configure Spring MVC's CORS to allow headers Accept and Content-Type on all /api mappings, as we're exposing REST endpoints
}
